{% extends '../layout.twig' %}


 {% block app_content %}
     <component is="login-provider"
             class="login">
         <component is="login-form"
                 :is-logged-id="isLoggedId"
                 slot-scope="{isLoggedId}"
                 title="Login"></component>
     </component>
 {% endblock %}
