
import * as getters from './getters';
import mutations from './mutations';
import actions from './actions';

const state = {
    branch: null,
};

export default {
    state,
    getters,
    mutations,
    actions,
};
