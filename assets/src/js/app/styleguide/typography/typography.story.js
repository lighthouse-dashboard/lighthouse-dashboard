import Vue from 'vue';
import Typography from './typography.vue';

export default { title: 'Styleguide / Typography' };

Vue.component('typography', Typography);

export const component = () => '<typography/>';
