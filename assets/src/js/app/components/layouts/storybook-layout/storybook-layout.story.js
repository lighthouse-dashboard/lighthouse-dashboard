import StorybookLayout from './storybook-layout.vue';

export default { title: 'Layouts / storybook-layout' };

export const component = () => ({
    components: { StorybookLayout },
    props: {

    },
    template: '<storybook-layout />',
});
