import SitesList from './sites-list.vue';

export default { title: 'sites-list' };

export const component = () => ({
    components: { SitesList },
    props: {

    },
    template: '<sites-list />',
});
