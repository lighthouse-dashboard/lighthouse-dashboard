import CloseBtn from './close-btn.vue';

export default { title: 'Components / close-btn' };

export const component = () => ({
    components: { CloseBtn },
    props: {

    },
    template: '<close-btn />',
});
